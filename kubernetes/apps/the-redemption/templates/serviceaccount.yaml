# No role-arn annotation: authenticates via EKS Pod Identity, not IRSA
# The pairing is an aws_eks_pod_identity_association, not in this manifest.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "the-redemption.fullname" . }}
  labels:
    {{- include "the-redemption.labels" . | nindent 4 }}
