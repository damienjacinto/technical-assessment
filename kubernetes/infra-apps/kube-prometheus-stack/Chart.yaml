apiVersion: v2
name: kube-prometheus-stack
description: Wrapper chart pinning upstream kube-prometheus-stack for GitOps deployment via ArgoCD
version: 0.1.0
dependencies:
  - name: kube-prometheus-stack
    version: "88.3.0"
    repository: "https://prometheus-community.github.io/helm-charts"
