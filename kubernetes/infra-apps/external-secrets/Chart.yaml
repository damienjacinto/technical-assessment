apiVersion: v2
name: external-secrets
description: Wrapper chart pinning upstream External Secrets Operator + an example SecretStore for GitOps deployment via ArgoCD
version: 0.1.0
dependencies:
  - name: external-secrets
    version: "0.10.4"
    repository: "https://charts.external-secrets.io"
